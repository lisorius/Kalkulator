# Нейросеть попросила сделать калькулятор в виде задания я сделал хоть он и преметивен
a = int(input('введите 1 любое целое число\n'))
b = int(input('введите 2 любое целое число\n'))
c = input('какую операцию вы хотите выполнить?\n')
if c == '-' : print(a - b)
elif c == '+': print(a + b)
elif c == '*': print(a * b)
elif c == '/':
    if b == 0: print('Ошибка деления на ноль')
    else: print(a/b)
else:print('данных операций нет в базе данных по этому их невозможно выполнить вот какие имеються: сложение(+), вычитание(-), умножение(*) или делениее(/)')
